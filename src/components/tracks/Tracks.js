import React, { Component } from "react";

class Tracks extends Component {
  render() {
    return (
      <React.Fragment>
        <h1>Tracks</h1>
      </React.Fragment>
    );
  }
}

export default Tracks;
